export interface Course {
  id: string;
  title: string;
  description: string;
  topic: string;
  level: string;
  sessionCount: string;
  createdAt: string;
  groupCount: number;
  studentCount: number;
}